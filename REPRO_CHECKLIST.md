# Reproducibility Checklist Status

## Environment blockers
- Python dependencies required by the figure/table scripts are not present, and `pip install` is blocked by the proxy (see `pip install numpy pandas matplotlib`).

### What to add to the environment
- Python 3.10+
- Python packages (pip installable):
  - `numpy`
  - `pandas`
  - `matplotlib`
  - `seaborn` (used in notebooks for nicer plots)
- Jupyter stack for notebooks:
  - `ipykernel`
  - `nbformat`
  - `nbclient`
- Optional but helpful system packages for reproducible figure builds:
  - `texlive-full` (only needed if regenerating LaTeX/PDF artifacts locally)
  - `ghostscript` (PDF tooling)
  - `poppler-utils` (PDF comparison utilities like `pdfinfo`, `pdftocairo`)

## Figures
- **Figure 1 — CSK Radar**: Generated by `core/scripts/make_figs.py` (produces `fig1_aggregator_means_ci.pdf`); execution is currently blocked because numpy is missing. Re-run once dependencies are available and compare the medians/CI bands to the PDF.
- **Figure 2 — Frontier Diagram**: LaTeX-only; no code rerun needed.
- **Figure 3 + 4 — Metallic Convergence + Error Decay**: Backed by `phi demo/phi_proof_harness.py` (audits GEC0 pairs, fits metallic/null models, bootstraps CIs). Requires numpy/pandas; unable to execute without dependencies.
- **Figure 5 — Aggregator Fixed-Point CI**: Generated by `phi demo/phi_aggregator_runner.py` against the demo CSVs; also requires numpy/pandas, so regeneration is pending dependency installation.
- **Figure 6 — MSE to φ**: Part of `core/scripts/make_figs.py` (writes `fig2_mse_vs_phi.pdf`). Same dependency block as Figure 1.
- **Figure 7 — GEC Across Domains**: Values live in `core/notebooks/Compression_Physics_GEC_CaseStudies.ipynb`; notebook execution is pending once Python deps are installed.

## Tables
- **Table 3 — Case Studies**: Notebook-derived from `core/notebooks/Compression_Physics_GEC_CaseStudies.ipynb`; needs execution to confirm exact values.
- **Table 4 — Robustness Grid**: Built by `core/scripts/make_table-vi.py`; runs once numpy is available and should be compared to the PDF grid.
- **Table 5 — Aggregator Robustness**: Comes from `phi demo/phi_aggregator_runner.py`; rerun on the provided demo CSVs after installing dependencies.
- **Table 6 — DEA Sensitivity**: Notebook-derived (see `core/notebooks/GEC_Toolkit_Pro.ipynb` / `GEC_Compression_Physics_Toolkit.ipynb`); awaiting notebook execution with dependencies installed.
